# 2026-05-26

# JDBC Source : agregados

- Fase 1 : cargas <mark style="background: #FF5582A6;">sin</mark> distorsión (RAW)
- Fase 2: Transformaciones

```SQL
create stream s_customer(
customer_id INT
...
)
WITH (kafka_topic='sak-customer', value_format='AVRO', TIMETSTAMP='last_update')

-- Agregados
select store_id, count(*) as n 
from s_customer 
group by store_id 
emit changes; -- OJO query tipo pull; estado de conteo, parecido a estados de agregacion en Clickhouse

CREATE TABLE c_store AS
select store_id, COUNT(*)
FROM s_customer
GROUP BY store_id
EMIT CHANGE;

-- PULL ?¿?
select * from c_store where store_id=1;

```

>[!Warning] Ojo con sink
>clave primaria, puede haber muchas filas con claves primarias. 

RIUP (read, insert, update, p???) pero ahora tenemos UPSERT


# JDBC SINK

Cogemos mensajes y llevar a relacional.

```SQL
CREATE STREAM songs (
...
)
WITH (KAFKA_TOPIC="songs", PARTITIONS=1, VALUE_FORMAT='AVRO')

--- LLAVE DE PASO
INSERT INTO songs
SELECT *
FROM raw_songs EMIT CHANGES;
--- Controlamos el stream songs
```

>[!Quote] Partitions
>Paraleliza el procesamiento de los topicos

>[!Danger] NOSOTRO PONEMOS
>PARTITIONS = 1
>REPLICAS = 1

>[!Danger] 
REPASO EE1 - se podria hace con kafka
>- mariaDB
> - con kafka (jdbc)
> - parquet
> - JSON
>	- con kafka (file pulls)
>
> Dos streams raw -> fundo en 1 (joins)
> Stations - crear tabla
> Servicios - stream - stream partitioned


Clave primaria:
 -Forma 1
- AS_VALUE
	- lo pone en key ?

```SQL
CREATE TABLE AlbSong1
WITH (KAFKA_TOPIC='albsong1', key_format='AVRO', Value_format='avro')
AS SELECT
```

>[!Danger] campos del groupby en clave

k1,k2 \ artist \album \n

https://kafka.datastack.seibe.org/Tutorial/JDBCSink2/#clausula-as_value

- Forma 2
	- claves estructuradas

>[!Quote] 
>C1 C2
>V1 V2
>
>C1C2


>[!Danger] Afecta al conector SINK acorde con la forma

>[!Danger] Ver la documentacion de los conectores

# Partition by

lo que viene alli viene el el key

>[!Danger] importante

# JOINS


Stream vs Tabla
- stream refleja el flujo (secuencia de movimiento)
- tabla (tablero ajedrez)
	- normalmente asociados a agregados
	- estado concreto

>[!Danger] Mejor hacer varios stream, uno por cada transformacion
>no me importa tener un monton de streams por mensaje

https://kafka.datastack.seibe.org/Tutorial/Joins/

Para unir streams estos deberán estar coparticionados (ver información detallada en [Co-partitioning Requirements](https://docs.confluent.io/platform/current/ksqldb/developer-guide/joins/partition-data.html#co-partitioning-requirements)) lo que significa que los registros de entrada de ambos lados de la unión tienen el mismo esquema de particionadolos mismos ajustes de configuración para las particiones.
>[!Danger] deben de compartir la clave



# Ejemplo de ejercicio

ejecutar python en el docker para generar datos
parametros

>[!Danger] Buscar web que parsee json

# Clickhouse

El raw de clave de paso puede tener menos elementos.

![Drawing 2026-05-26 13.10.17.excalidraw](Drawing%202026-05-26%2013.10.17.excalidraw.md)

>[!Danger] Ojo clave simple de tipo struct
>![](Pasted%20image%2020260526131253.png)


| C1  | C2  |
| --- | --- |
|     |     |

vs


| C1C2 |     |
| ---- | --- |
|      |     |
>[!Danger] hay rebobinado

>[!Danger] Una consulta vive el tiempo que tarda en procesarse. En streaming la consulta es un proces en tiempo continuo, basicamente no acaba nunca, espera otros datos
>![](Pasted%20image%2020260526132804.png)

El insert no pararia. 

Se puede parar antes el flujo.
- parar el conector (DROP CONNECTOR, PAUSE CONNECTOR)
- DROP STREAM
	- light
	- hard
	- drop stream, delete topic

>[!Quote] Para nosotros de pasar de streaming son diferentes mensajes (en realidad no)

>[!Quote] Engine Kafka es como una vista materializada

```SQL
DROP TABLE ibd00.kafka_demoips; CREATE TABLE ibd00.kafka_demoips ( N Int32 ) 

ENGINE = Kafka() SETTINGS kafka_broker_list = 
'redpanda:9092', 
kafka_topic_list = 'demoips', 
--- grupo de consumo
kafka_group_name = 'clickhouse', 
--- ojo
format_avro_schema_registry_url='http://schema-registry:8081',
--- ojo
 kafka_format = 'AvroConfluent';
```

>[!Warning ] Grupo de consumo
>Grupos de consumidores 
>por ejemplo cada equipo un group_name
>si se pone el mismo nombre para todos los equipos si un equipo ve el mensaje el otro no ya no lo puede ver


# Ejercicios

Intentar EE1 y EE2 con esto

Alex : (filtar en el where de clave de paso con el servicio)

CSV - KAFKA - Clickhouse
ver como

Connectores:
-file
-s3 minio
-jdbc source
-jdbc sink

# EXAMEN FINAL

>[!Danger] A partir del jueves 11 no contesta dudas
>

>[!Danger] Mismo dominio, pero no exactamente el mismo datos (dataset parecido) (que puede añadir y crear columnas)

- Datos vs metadatos
- Los resultados no son la Biblia
- Modalidades (se puede hacer el global aunque no sea necesario)
	- Progresiva
		- Contabiliza resultados progresivas
		- ee1 25%
		- ee2 30%
		- Npa 15%
		- ee3 Resto
			- 50% minimo
		- Teoria : Todo
		- Practica : 
			- EE1: nada de polars, dataframes, jupyter
			- EE2 + Kafka
	- Global (50% minimo)
		- Teoria : 99 % cuestionario (1 ejercicio) entra TODO
		- Practica : varios ejercicio
			- entra todo
			- (ojo ejecutar python para insertar kafka)

# Eficiencia EE2

- Leer todos los datos
- Join
- Group BY
- Leerlos varios veces