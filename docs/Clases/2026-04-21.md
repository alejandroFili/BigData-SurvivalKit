>[!Danger] Desde duckDB me puedo conectar a todos. En la misma ventana se puede hacer todo.
>Puede integrar cosas sin guardar nada en memoria.

>[!Danger] NO hay que usar vistas o with

En ee1 hay dos fuentes mariadb para estaciones, parquet hive (carpeta local ) para servicios 

```SQL
CREATE SECRET mariadb {
TYPE mysql,
HOST
PORT
DATABASE
USER
PASWWORD
};

ATTACH '' AS eurostar (TYPE mysql, SECRET mariadb);

FROM duckdb_secrets();

SELECT
FROM eurostar.eurostar.Stations

```

Aqui se connecta a s3 minio

```SQL
INSTALL httpfs;
LOAD httpfs;

CREATE OR REPLACE SECRET etsiinf {
TYPE s3
PROVIDER config,
KEY_ID
SECRET
ENDPOINT
URL_STYLE
REGION
USE_SSL true
}

FROM duckdb_secrets();

SELECT * FROM read_csv_auto("s3://sakstar/dim_date.csv")

DESCRIBE FROM

```


```
FROM read_parquet as p, eurostar.stations as s
```

```SQL
DESCRIBE
FROM read ('', hive_partitioning = true);
```

>[!Danger] Martes parecido con clickhouse

```SQL
SELECT ns as Semana, s.Country as Pais, count (DISTINCT ServiceID) as s1, count()
```

Buenas practicas

```
DETACH eurostar;
DROP SECRET mariadb;
DROP SECRET etsiinf;
FROM duckdb_secrets();
```


# Sobre visualizar

>[!Warning] Cuando hay logica de <mark style="background: #FF5582A6;">control de proceso</mark>, se necesita python. 
>El SQL es declarativo : en el basico no hay if, else etc

1. Producir informe
2. Visualizar informe

# Sobre clickhouse

INFILE : no se puede usar en remoto

Alternativa FILE

File table
```
DESCRIBE file
```


url table
s3 table


# Sobre mutaciones


# Estados de agregacion

https://bitacora.datastack.seibe.org/ArqBI/ClickHouse/AggStates/?h=

Agregados : funciones sobre datos


SELECT
FROM 
WHERE
GROUP BY
ORDER BY

Select tiene dependencia sintactica del group by si en group by estan A y B en select tiene que aparecer A, B si funciones agregadas de A y B. No se puede optimizar nada hasta que tenga el resultado.
En transcaccional relacional es infrecuente en relacional transaccional. En OLAP es muy comun.

>[!Danger] Los mergeTree han sido inventados para favorecer los groupBy

>[!Danger] Los estados de agregacion son especificos de clickhouse?

![Drawing 2026-04-21 13.36.24.excalidraw](Drawing%202026-04-21%2013.36.24.excalidraw.md)

TIpo de datos nuevo : AggregateFunction

>[!Danger] Permite adelantarse en el calculo de los agregados

>[!Warning] las vistas materializadas en clickhouse son pequeños pipelines

>[!Warning] No se puede consultar la tabla hasta cuando no acabe

Nuevo engine (hereda del merge tree): 

```
ENGINE = AggregatingMergeTree() ORDER BY (EventDate, EventID);
```

## CountIF

se puede hacer agrupaciones condicionales.

## States

Ahora tenemos:
avg-State vs avg-Merge

![](Pasted%20image%2020260421135627.png)
2 no es valida
3 es valida

# Advanced SQL

https://bitacora.datastack.seibe.org/Operacional/AdvSQL/?h=sql#views

Vista : consulta virtual (ojo con los permisos), normalmente no tiene almacenamiento.

>[!Danger] Ojo con vistas materializadas y se actualizan los datos, puede ser incongruenes

```SQL
CREATE MATERIALIZED VIEW
```

materializaed : guarda el resultado (hay almacemanient)

>[!Danger] Trigger : muy mala idea usar on operacional
>Los triggers son sincronos (se para la concurencia del gestor)


tablas temporals : una especie de vistas

