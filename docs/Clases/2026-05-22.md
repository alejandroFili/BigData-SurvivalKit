# 2026-05-22

# Owlshop

>[!Warning] No hay que crear connectores

>[!Danger] Hacer lo de cambiar los topics a stream
>mapear stream y tablas

```SQL
CREATE STREAM 'raw_customers' (
'version' INTEGER, 'id' STRING, first...
)
WITH (kafka_topic = 'owlshop-customers', value_format="JSON");

describe 'raw_customers' extended;

CREATE STREAM 'raw_orders' (
'version'
...
ARRAY<STRUCT<'articleID' STRING ... > >
)
WITH (kafka_topic='owlshop-orders', value_format="JSON")
```

>[!Danger] Te puede meter datos en key tambien

>[!Danger] Ojo que pueda que el integer de parquet no sea compatible con el integer de la base de datos
>metadatos etc para ver los tipos

>[!Warning] RAW - sin transformacion; respectar la estructura de los datos

>[!Quote] describe 'raw_customers' extended;
>metadatos:
>- DDL
>- 

>[!Danger]
Insert - genera datos
DDL - genera metadatos

>[!Quote] Dashboard jupyter con el consumidor

```
print 'adw.seibe.acceso' from beginning limit 1
```

# REcap

msyql jupiter
- mysql server
- mariadb 
- cualquier otro con minimos cambios



# Ejemplo conector S3 source

```SQL
--- depende del tipo de conector
connetor.class = s3.source ....
confluent.topic.boostrap.servers = "broker:9092"
storage.class = s3.storage
--- depende de los datos
format.class = io.confluent.connect.s3.format.avro.AvroFormat
tasks.max = 1
s3.bucket.name = "kafka"
s3.region= 
aws.acces.key.id = ""
aws.secret.access.key = ""
store.url = "http://izar.ls.fi.upm.es:30009"
--- ojo estilo particionado en parquet
partitioner.class = ".... TimeBasedPatitioner"
path.format = "YYYY/MM/DD"
--- para 2026
--- path.format ="2026/MM/DD"
key.converter = .AvroConverter
--- ahora el propio broker hace el schema registry
--- es redpanda:8081
key.converter.schema.registry.url = 'http://schema-registry:8081'
--- sin mas, no es un esquema creado por nosotros, es por defecto
key.converter.schemas.enable = 'false'
value.converter
value.converter.schema.registry.url
value.converter.schemas.enable



```

>[!Warning] es con quotes "task.max" = "1" etc


```
docker logs ksqldb-server
```


>[!Danger] Pone el stream desde el principio
```SQL
SET 'auto.offset.reset' = 'earliest';
print "nombre del topico" from beginning;
```

# Tarea

## Musiteca

kafka-connect-file-pulse

>[!Danger] NO usar API REST


>[!Danger] comilla `para que se quede con lo que queremos`


>[!Warning] Trasformaciones despues de carga de datos, dejar el raw sin distorsiones
>hacer en stream las transformaciones
>jode kafka, por que ya no es independiente lo de poner los tipos

Modificaciones comose
- poner volumen para tener acceso a la carpeta del server
- llaves de paso
	- pone otra carpeta en el connector
		- no deberia cambiar nada hasta que se copie alli
			- mas facil para ver los logs
- sobre truco streams
	- crear otro stream test que no lee nada, y hago insert
	- raw ---- normal_stream
		- ---- test_stream

>[!Danger] En principio fijo, no ficheros nuevos, que pasaria si entrarian

SpoonMan ???

ojo activar rebobinado SET 'auto.offset.reset'='earliest';
`print 'connect-file-pulse-csv';` 

Key Format
Value Format : AVRO
>[!Danger] Tal como esta configurado carga csv como AVRO

>[!Warning] Tipos de tiempo estan jodidos
>casi mejor coger los string, y despues hacer cosas en kafka para tener el tiempo



# JDBC

Dbeaver

![Drawing 2026-05-22 11.43.43.excalidraw](Drawing%202026-05-22%2011.43.43.excalidraw.md)

>[!Danger] Existe valor y clave, que tambien es informacion

>[!Danger]
>>Key format KAFKA_STRING
> - te pone key:1 aunque es un string
> Key format KAFKA_INT

No hay funciones de ventana o cte pero hay group by

```SQL
WITH(kafka_topic, value_format, TIMESTAMP='last_update')
--- last_update es del campo del propio mensaje
--- si son muy viejos se eliminan automaticamente si el campo es muy viejo
--- tambien lios con husos horarios
```



