# 2026-05-19

# Recap

!!! warning " Usamos Redpanda "

!!! danger " Importancia de los metadatos "
>Como debo interpretar lo que hay alli

Propuesta con una arquitectura distribuida y en cluster.

Componentes principales (cada uno puede ser clusters):
- Core (docker compose : service : redpanda)
	- Broker
		- donde se almacenan los datos
		- los datos son mensajes (eventos)
		- generación de datos continua
	- Controlador
		- Kraft (antes se usaba zookeeper)
		- ordenacion y sincronizacion de los elementos
	- Schema registry
		- diccionario de datos de la plataforma
		- almacena los metadatos que necesita la plataforma para ejecutar la tarea
		- permite integrar con otras cosas
		-  - --schema-registry-addr internal://0.0.0.0:8081,external://0.0.0.0:18081
- Connector
	- puede llegar a ser el cuello botella -> por eso cluster
	- base de datos de drivers
	- permite producir mensajes o consumir mensajes
	- Dos tipos
		- conectores tipo fuente
			- producir mensaje
			- jdbc : relacional
			- s3 : s3 minio
			- csv 
			- etc
		- conectores tipo sink
			- sacar mensaje de la plataforma

!!! danger " Docker Compose.yml - Metadatos "

!!! danger " Que son "AVRO" .... (tipo de fichero) "


!!! warning " Nos gustaria tener todo como una base de datos OLAP "
>Componente : 
>ksql-dbserver  (docker compose) (procesamiento de queries contra el almacenamiento de kafka)
>ksqldb-cli (docker compose) permite hacer consultas contra el dbserver
>Permite pasar del nivel de los mensajes a nivel de filas de un resultado SQL

!!! danger " Ejemplo año pasado trenes "


![Drawing 2026-05-19 12.33.03.excalidraw](Drawing%202026-05-19%2012.33.03.excalidraw.md)

## Recap mensajes

Se puede meter info en las dos partes : key y value

!!! warning " Topics : etiqueta que permite agrupar los mensajes "
>hay roles etc para que las aplicaciones pueda generar mensajes en un topico en concreto

Topics son hierarquicos :
- etsiinf_asign1
- estiinf_asign2

```
print 'tw_raw' from beginning limit 1;
```

Imprimeme los mensajes de ese topicos desde el comienzo, solo 1
!!! warning " Es un mensaje a nivel almacenamiento "

!!! danger " Info valiosa "
>Resultado del print anterior: 
>`print 'tw_raw' from beginning limit 1; `
KEY FORMAT
VALUE FORMAT

!!! quote " Key no tiene que ver con clave primaria "

!!! quote " Cada consumidor tiene un hilo temporal diferente ¿? "

# KSQLDB

Nivel de almacenamiento : mensajes y topicos
Nivel SQL tres abstracciones:
- Connector:
	- borrar y crear conectores
		- dialecto tipico SQL para connectors
		- si creo un connector source
			- se empieza a producir mensajes.
		- si creo un connector sink
			- se empieza a consumir mensajes
	- streams
		- representa el flujo de los mensajes dentro de un topico
		- se pueden tener muchos funcionando a la misma vez
		- metafora: tubo por donde fluye el agua
		- <mark style="background: #FF5582A6;">siempre crear un raw_stream</mark>
			- que tipo
			- como parseo
			- interactua directamente con los mensajes
		- encadeno streams, transformando los datos
			- se puede crear pipelines a traves de streams
	- tablas
		- no tienen nada de ver con las tablas de SQL
		- es un estado concreto de un stream (o varios streams)
		- solo vale para hacer aggregados
		- metafora : ajedrez
		- https://docs.confluent.io/platform/current/streams/overview.html#related-content
		- mientras evalua se cambia

!!! danger " Importancia de tener la tabla RAW; interpretar bien metadatos "
>StopArrivalTime : 
>Service ID : no puede ser nulo


![Drawing 2026-05-19 12.53.56.excalidraw](Drawing%202026-05-19%2012.53.56.excalidraw.md)

# Queries

3 tipos:
- puramente continuas
- PUSH
	- van contra datos en streams
	- EMIT CHANGES (da error si no la pones)
	- reciba mensaje con cada cambio
- PULL
	- (como las tablas)
	- devuelven el estado actual y terminan
	- requieren tablas en estado materializado

# Extra

```
print logstash from beginning limit 2;
```

Key format : no data processe (no hay info util,vacio)
Value format : JSON

```SQL
CREATE STREAM seibe_apache_raw (
WITH (
KAFKA_TOPIC = "logstash"
VALUE_FORMAT = "JSON");
)
)
```

Ahora se puede hacer 

```
SELECT timestamp, "@timestap", tag from seibe_apache_raw WHERE etc
```

!!! danger ""
PASO 1: CONNECTOR
PASO 2: CREATE STREAM RAW (ver metadatos)


!!! danger " Pueden tener campos compuestos "

# Demo

todos son json
Copiar y pegar /n que no deberian aparecer
jq - hacer consultas a json

PARSEAR
- json (mapear json de por si es capaz)
- protobuff - inyectar en el squema registry para poder tratarlos el esquema del tipo de fichero (los metadatos)
- avro

lineitems: array

# Enlace con tutorial

!!! danger " Objetivo : hay que completarlo, dificil examen si no (posible errores) "

Musiteca (como crear conector fuente, para cargar datos del csv)
- ejemplo, datos csv
- crear conetor csv e injectar en el cluster
	- dos maneras 
		- API REST (prohibido usarlo)
		- Mirar parametros
- list topic
JDBCSource

alias :
docker exec -it ksqldb-cli ksql  ...server:8088
los csv deberian estar en ksqldb_server
- cp
- o bound volume

# Viernes

Dos primeras partes del tutorial.

