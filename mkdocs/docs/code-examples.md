# Code example

```py title="Example" linenums="1" hl_lines="2-2"
# Function to add two numbers
def add_two_numbers(num1, num2):
    return num1 + num2

# Example usage
result = add_two_numbers(5, 3)
print('The sum is:', result)
print(==)
```

```sql
FROM table SELECT a,b,c,

```